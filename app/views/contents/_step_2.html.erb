<div class="fields step-2">
  <h2 class="fs-title">Locale</h2>
  <h1 class="fs-subtitle">Choose the locale for this content.</h1>
  <div class="fields">
    <div class="field">
      <% locales = ["hy", "zh", "nl", "en", "eo", "fr", "ka", "de", "el", "it", "ja", "ko", "ku", "fa", "pl", "pt", "ru", "es", "sv", "tr"] %>
      <%= f.label :locale, class: "required" %>
      <% if content.locale.present? %>
        <% l = content.locale %>
      <% else %>
        <% l = "en" %>
      <% end %>
      <%= f.select :locale, options_for_select( locales, l), {}, { class: "selectpicker show-tick", autocomplete: "off", 'data-width' => "100%", 'data-header' => "Select a Locate", 'data-live-search' => "true" } %>
      </field>
      <field>
        <label class="block required">Locale Index</label>
        <%= f.hidden_field :is_index, value: false %>
        <% if content.is_index.present? &&  content.is_index == true %>
          <%= f.text_field :is_index, class: "js-checkboxpicker", type: "checkbox", checked: true, value: "true",
                          data: { off_label: "No", on_label: "Yes", off_active_cls: "btn-warning", on_active_cls: "btn-primary",
                                  on_title:"Yes", off_title: "No", switch_always: true } %>
        <% else %>
          <%= f.text_field :is_index, class: "js-checkboxpicker", type: "checkbox", value: "false",
                            data: { off_label: "No", on_label: "Yes", off_active_cls: "btn-warning", on_active_cls: "btn-primary",
                                    on_title:"Yes", off_title: "No", switch_always: true } %>
        <% end %>

    </div>
  </div>
</div>

<hr>

<div class="wrap-panel-alert">
  <div class="alert alert-info alert-dismissable">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
    Info! Content is grouped by it's locale flag. The default locale is en. </br>
          Each locale group may has one index/default page. If no index page has been defined the one with highest priority will be set. </br>
  </div>
  <div class="alert alert-warning alert-dismissable">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
    Warning! By defining a page as an index any previous set indexes will be lost. </br>
  </div>
</div>
