<nav class="navbar navbar-default ">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#js-hotel-navbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">HotelName</a>
    </div>
    <div class="collapse navbar-collapse" id="js-hotel-navbar">
      <ul class="nav navbar-nav">
        <% if @index.title == @content.title %>
          <li class="active"><%= link_to "#{@index.title}", hotel_show_path(:id => @index.id), class: "nav-link nav-0%"%></li>
        <% else %>
          <li><%= link_to "#{@index.title}", hotel_show_path(:id => @index.id), class: "nav-link nav-0%"%></li>
        <% end %>

				<% @navbar.each_with_index do |nav, index| %>
          <% if nav.title == @content.title %>
            <li class="active"><%= link_to "#{nav.title}", hotel_show_path(:id => nav.id), class: "nav-link nav-#{index}"%></li>
          <% else %>
            <li><%= link_to "#{nav.title}", hotel_show_path(:id => nav.id), class: "nav-link nav-#{index}"%></li>
          <% end %>
				<% end %>
        <!--<li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown">Page 1 <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Page 1-1</a></li>
            <li><a href="#">Page 1-2</a></li>
            <li><a href="#">Page 1-3</a></li>
          </ul>
        </li>-->
      </ul>
      <% unless user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
  				<li><%= link_to new_user_registration_path(:locale => "en") do %><span class="glyphicon glyphicon-user"></span> Sign Up <% end %></li>
  				<li><%= link_to new_user_session_path(:locale => "en") do %><span class="glyphicon glyphicon-log-in"></span> Login<% end %></li>
          <li class="readme btn"><i class="fa fa-book"></i></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>

<div id="readme" class="hidden" >
  <%= render partial: "shared/readme" %>
</div>
