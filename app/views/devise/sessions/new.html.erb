<div class="login">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="fields">
    <%= link_to hotel_index_path do %>
      <span class="btn btn-default btn-home" title="Hotel Page"><i class="fa fa-home"></i></span>
    <% end %>

    <hr />
    <h4>Log in</h4>
    <hr />
    <section class="alert alert-danger">
      <p>
        New users will need approval from an admin account and since I won't be monitoring
        this demo there is no point signing up so just log in using the test user account
        I have set up with these credentials.
        <br>
        <br>
          mail: <b>test_user@demo.com</b> and password: <b>p4$$w0rD</b>
        <br>
        <br>
          After you log in you can test most of the administration interface and it's functionalities.
        </br>
        </br>
        <b>Just please do so responsibly and try not to break the demo so other people can use it as well.</b>
      <p>

      <p>
        <em>
           This information was originally included in the readme file,
           usage section, which seems nobody has ever read, even after
           I made an annoying pop up modal out of it, so here it is.
         </em>
      </p>

    </section>

    <%= devise_error_messages! %>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>

    <div class="input-group field">
      <span class="input-group-addon" title="Email"><i class="fa fa-envelope"></i></span>
      <!-- <%= f.label :email %><br /> -->
      <%= f.email_field :email, autofocus: true, placeholder: "Email" %>
    </div>

    <div class="input-group field">
      <span class="input-group-addon" title="Email"><i class="fa fa-lock"></i></span>
      <!-- <%= f.label :password %> -->
      <%= f.password_field :password, autocomplete: "off", placeholder: "Password" %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me, class: "remember-me" %>
        <%= f.label :remember_me %>
      </div>
    <% end -%>
  </div>

  <div class="footer">
    <div class="actions">
      <%= button_tag type: 'submit', class: "btn btn-primary" do %>
        <i class="fa fa-sign-in" aria-hidden="true"> Log in</i>
      <% end %>
      <!-- <%= f.submit "Sign up", :class => "btn btn-primary" %> -->
      <%= render "devise/shared/links" %>
    </div>
  </div>
  <% end %>
</div>

<%= javascript_tag do %>
   $('.remember-me').checkboxpicker();
<% end %>
